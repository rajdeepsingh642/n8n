apiVersion: v1
kind: Service
metadata:
  name: n8n-demo-app-service
  labels:
    app: n8n-demo-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30000
    protocol: TCP
  selector:
    app: n8n-demo-app
